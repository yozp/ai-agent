package com.yzj.aiagent.constant;

/**
 * 文件常量类
 */
public interface FileConstant {

    /**
     * 文件保存目录，为项目根目录下的 /tmp 目录中
     */
    String FILE_SAVE_DIR = System.getProperty("user.dir") + "/tmp";
}
